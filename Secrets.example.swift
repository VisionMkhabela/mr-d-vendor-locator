import Foundation

enum Secrets {
    /// Copy this file to `MrDVendorLocator/App/Secrets.swift` and paste your key.
    /// `Secrets.swift` is gitignored and must not be committed.
    static let googleMapsAPIKey = "YOUR_GOOGLE_MAPS_API_KEY"
}
